<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shareshipment">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Always include this permission -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Include only if your app benefits from precise location access. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AnnouncedShipments"></activity>
        <activity android:name=".AssignShipment" />
        <activity android:name=".SignIn" />
        <activity android:name=".AvailableShipments" />
        <activity android:name=".DeliverShipment" />
        <activity android:name=".CompleteWindow" />
        <activity android:name=".ConfirmRecipientInformation" />
        <activity android:name=".ChooseRecipient" />
        <activity android:name=".AnnounceShipment" />
        <activity android:name=".MainFunctionality" />
        <activity android:name=".Password" />
        <activity android:name=".ActivationCode" />
        <activity android:name=".SignUp" />
        <activity
            android:name=".WelcomeActivity"
            android:parentActivityName=".MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>